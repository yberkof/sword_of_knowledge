services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: sword
      POSTGRES_PASSWORD: sword_dev
      POSTGRES_DB: sword_of_knowledge
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
