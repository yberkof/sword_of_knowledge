apiVersion: v1
kind: Service
metadata:
  name: sok-socket
spec:
  selector:
    app: sok-socket
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: socket
      port: 8081
      targetPort: socket
